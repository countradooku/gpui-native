{
  "name": "@gpui-react/example-showcase",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run typecheck && vite build --config ../../vite.react-examples.config.mts",
    "build:binary": "bun run build && bun run ../../scripts/compile-example.mts",
    "start": "bun dist/main.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@gpui-native/react": "workspace:^",
    "react": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/react": "^19.2.0",
    "typescript": "catalog:",
    "vite": "catalog:"
  }
}
